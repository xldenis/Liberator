<% content_for :edit_menu do %>

<div id="layout-menu">
	<ul id="list">
		<%=render :partial => "article_list" %>
	</ul>
</div>

<% end %>
<% begin %>
<%= render :partial => "categories/edit/#{@category.name.downcase}" %>
<% rescue %>
<div class="content">
	<%= render :partial =>"index",:locals => {:articles => @front_page_articles} %>
</div>
<% end %>